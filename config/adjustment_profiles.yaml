# DEPRECATED — file retained for archival reference only.
#
# All adjustment profiles (Stage 1 definition alignment, Stage 2
# bank/private-credit adjustments, trading_history, invoice_concentration,
# lgd_specific) have been moved to downstream consuming projects.
#
# Rationale: the external benchmark engine's responsibility is to publish
# raw, source-attributable PD/LGD observations. Adjustments are use-case
# specific (PD workbook applies different adjustments than LGD or stress
# testing) and belong in the consuming project so each use case can manage
# its own complete adjustment chain coherently.
#
# Where to find the adjustments now:
#   * PD-related adjustments → Thin Data Workbook (PD model), Sheet 07 LRA
#     calibration with EBA Margin of Conservatism categories A/B/C
#   * LGD-related adjustments → LGD project (when built)
#   * Stress testing overlays → Stress testing project (when built)
#
# Last engine commit using this file: <commit hash to be filled in>
adjustment_profiles_deprecated: true
